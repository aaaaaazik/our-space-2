import { redirect } from "next/navigation";

/**
 * Планы переехали в календарь.
 *
 * Страница осталась ради установленного приложения: у него старый адрес
 * может быть сохранён в истории и в кэше, и упереться в «страница не
 * найдена» было бы неприятно.
 */
export default function PlansPage() {
  redirect("/calendar");
}
